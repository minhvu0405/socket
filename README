Twitter streaming

* Installation
- Server:
		+ Python 2.7
		+ Flask framework: pip install Flask (http://flask.pocoo.org/docs/installation/)
		+ TwitterAPI (python package): pip install TwitterAPI (or https://github.com/geduldig/TwitterAPI)
		+ Gunicorn (HTTP server): easy_install -U gunicorn
	To run server, enable "virtualenv" then : gunicorn -k flask_sockets.worker server:app
		
- Client: 
		+ Angular
		+ D3
		
